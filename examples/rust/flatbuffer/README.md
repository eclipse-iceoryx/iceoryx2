# In-Progress

## Prerequesites

* Install flatbuffers:
  ```sh
  # archlinux
  pacman -S flatbuffers

  # debian/ubuntu
  apt install flatbuffers
  ```

## Usage

```sh
flatc --rust unbounded_data.fbs
```
